import { ProjectCategoryDTO } from './project-category-dto';

describe('ProjectCategoryDTO', () => {
  it('should create an instance', () => {
    expect(new ProjectCategoryDTO()).toBeTruthy();
  });
});
